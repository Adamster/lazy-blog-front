/**
 * Category-chip label for a post with NO tag — the deadpan NOT LAZY shrug
 * (renders `[ WHATEVER ]`). We didn't tag it, we're not going to, and that's a
 * feature. Shared so the feed card + the post page never drift apart.
 */
export const UNTAGGED_LABEL = "whatever";
