/** The two edit-profile sections — tabs, not steps (free-switch, no gate). */
export type ProfileTab = "profile" | "security";

export const PROFILE_TABS: readonly ProfileTab[] = ["profile", "security"];
