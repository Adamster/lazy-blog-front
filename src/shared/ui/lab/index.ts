// LAB — the UNAPPROVED reserve. A dump for primitives/marks parked here until the
// owner approves them into the public lib. NOT exported from the core `@/shared/ui`
// barrel — import from `@/shared/ui/lab/<file>` directly. Nothing here ships by
// default; move a file UP into a category folder + the core barrel once approved.
export { LogoSloth } from "./logo-sloth";
